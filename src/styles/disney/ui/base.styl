html, body, #app, .layout {
  display: flex;
  flex-direction: row;
  flex: 1;
  box-sizing: border-box;
  min-width: 0;
}

.app-main {
  min-height: 100%;
}

.bg--gray {
  background: $color-light-grey-sss;
}

.el-radio-group {
  vertical-align: top;
}
